var app = angular.module("myapp", ["ngCookies"]);
app.controller("successCtrl",["$location","$timeout",function($location,$timeout){
    // $location.path("http://" + $location.host() + ":" + $location.port() + "/PHM");
}]);